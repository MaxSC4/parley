# Dialogue Sessions

Each player runs a session with a current label and index.

## Key Points
- One session per player at a time.
- Sessions end on `end` or EOF.

## Example
```lua
Parley.Start(player, asset, { entry = "start" })
```
